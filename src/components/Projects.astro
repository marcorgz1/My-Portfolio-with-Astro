---
// Objeto con la información de cada proyecto
const PROJECTS = [
    {
        title: 'Arkanoid Game',
        description: 'Lorem ipsum dolor sit amet, consectetur adipiscing elit. Cras vehicula turpis nisi, sed iaculis nisl congue eu. Nulla sodales tellus vitae nisi tristique malesuada. Proin malesuada fringilla metus at scelerisque. Curabitur eget rutrum diam. Pellentesque sed ullamcorper velit. Cras pulvinar ac augue eget maximus.',
        github: 'https://github.com/Markix1/Javascript-Projects',
        link: 'https://arkanoidgamejavascript.netlify.app/',
        image: '/projects/arkanoid_game.webp',
        tags: ['HTML', 'CSS', 'JavaScript']
    }
]
---
{
    PROJECTS.map((project) => (
        <article>
            <h3>{project.title}</h3>
            <p>{project.description}</p>
            <ul> 
            {
                project.tags.map((tag) => (
                    <li>{tag}</li>
                ))
            }
            </ul>
            <img src={project.image} alt={`Screenshot of the ${project.title}`}>
        </article>
    ))
}

{/* PROJECTS.map(({ title, description, tags } => {
        <article>
            <h3>{title}</h3>
            <p>{description}</p>
            <ul>
            {
                project.tags.map((tag => {
                    <li>{tag}</li>
                }))
            }
            </ul>
        </article> */}